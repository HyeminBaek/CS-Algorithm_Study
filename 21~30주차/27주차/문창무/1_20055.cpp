#include <iostream>
#include <vector>
using namespace std;

int n, k;  // 100, 200

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);

    cin >> n >> k;
    vector<int> v(2 * n);
    vector<bool> check(2 * n);
    for (int i = 0; i < 2 * n; ++i) {
        cin >> v[i];
    }

    int b

    return 0;
}
